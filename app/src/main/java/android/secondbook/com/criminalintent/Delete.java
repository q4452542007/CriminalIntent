package android.secondbook.com.criminalintent;

import android.content.Context;

import java.util.List;

/**
 * Created by Administrator on 2017/8/31.
 */

public class Delete {

  /*  private Context mContext;

    public Delete(Context context,Crime mCrime) {
        mContext = context;
        CrimeLab.get(mContext).deleteCrime(mCrime);
    }

    private int findCrime() {
        int num;
        for (int i = 0; i < crimes.size(); i++) {
            if (crimes.get(i).getTitle() == null) {
                num = i;
                return num;
            }
        }
    }

    private void isTitleEmpty(List<Crime> crimes) {

        for (int i = 0; i < crimes.size(); i++) {
            if (crimes.get(i).getTitle() == null) {
                crimeLab.deleteCrime(crimes.get(i));
                crimes.remove(i);
            }
        }
    }*/
}
